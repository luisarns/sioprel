<?php 	session_start();		header("Content-Type: text/plain");		require_once 'Configuracion.php';	require_once 'FunDivipol.php';		$coddivipol = $_POST['coddivipol'];	$codnivel   = $_POST['codnivel'];		$datos['coddivipol'] = $coddivipol;	$datos['codnivel']   = $codnivel;	$_SESSION['consuListadoElegidos'] = serialize($datos);		//Incluir la funcion que hace el procesamiento y genera los resultados de la consulta	require_once('consulListadoElegidos_inc.php');		//En este punto se recorre el resultado de la base de datos	//y se llena el arreglo con los resultados que seran enviados 	//al cliente		$datos = array();	// while($row = ibase_fetch_object($result)){		// $listLista = array();		// $listLista['codigo']    = $row->CODIGO;		// $listLista['nombres']   = htmlentities($row->NOMBRES);		// $listLista['apellidos'] = htmlentities($row->APELLIDOS);		// $listLista['partido']   = htmlentities($row->DESCRIPCION);		// $listLista['votos']     = $row->VOTOS;		// array_push($arrListListas,$listLista);	// }		// ibase_free_result($result);	// ibase_close($firebird);		//para la prueba de interaccion con el cliente	//Falta agregar la configuracion para hacer la consulta	echo json_encode($datos);	?>